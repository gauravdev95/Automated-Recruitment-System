# Python 3.10 or 3.11 recommended.
#   - sentence-transformers / torch 2.2.0 ship no wheels for Python 3.12+,
#     so pip may try to build torch from source and fail.
#   - torch is pulled from the CPU-only index (smaller than the CUDA bundle).
fastapi>=0.110,<1.0
uvicorn[standard]>=0.29,<1.0
pdfplumber>=0.11,<1.0
sentence-transformers>=2.6,<4.0
scikit-learn>=1.4,<2.0
numpy>=1.26,<2.0
python-multipart>=0.0.9,<1.0
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.2.0
